apiVersion: v1
kind: Namespace
metadata:
  annotations:
  labels:
    kubernetes.io/metadata.name: {{ .Values.environmentName }}-argo 
    platform/owner: {{ .Values.customerName }}
  name: {{ .Values.environmentName }}-argo
