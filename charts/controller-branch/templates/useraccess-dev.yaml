kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: admin-{{ .Values.environmentName }}-dev
  namespace: {{ .Values.environmentName }}-dev
  labels:
    kubernetes.io/metadata.name: {{ .Values.environmentName }}-dev
    platform/owner: {{ .Values.customerName }
subjects:
  - kind: Group
    apiGroup: rbac.authorization.k8s.io
    name: {{ .Values.groupNameAdmin }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: admin
